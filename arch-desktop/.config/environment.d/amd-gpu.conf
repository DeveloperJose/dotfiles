# This file is intentionally left blank to inherit global /etc/environment settings.
# Having duplicate or conflicting WLR_DRM_DEVICES here can cause issues.
# Global settings ensure the greeter (dms) and the user session use the same device.
