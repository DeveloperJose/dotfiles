# Make both NVIDIA and AMD GPUs available to Vulkan applications.
# Listing NVIDIA first makes it the default for high-performance requests.
VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json:/usr/share/vulkan/icd.d/radeon_icd.x86_64.json
